<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>SpaceSails.Labs.Lab31</RootNamespace>
    <InvariantGlobalization>true</InvariantGlobalization>
  </PropertyGroup>

  <!-- No ProjectReference. This lab does not fly anything: it is one exponential over body constants,
       and Beanstalk.cs is deliberately free of every dependency so that tests/SpaceSails.Core.Tests can
       LINK it and hold the printed verdicts to the same arithmetic that printed them. -->

</Project>
