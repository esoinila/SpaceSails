@namespace SpaceSails.Client.Pages
@implements IHandleEvent
@using System.Globalization
@using SpaceSails.Client.Rendering
@using SpaceSails.Contracts
@using SpaceSails.Core
@using SpaceSails.Core.Interior
@*
    #251 item 1 · THE GROUND LESSON CARD, ON ITS OWN FILE.

    Moved out of Map.razor by PURE MOTION — the block between the two fences below is the block that was in
    the page, character for character. Every page member it reads is a [Parameter] with the SAME NAME (the
    fields keep their underscore, the methods come across as delegates), which is what lets the markup move
    without a rewrite and lets the suite's source guards read it through MapMarkup exactly as before.

    The `@if` that opens it stays in Map.razor: the page keeps its paint order in one place, and a closed
    surface still renders nothing. IHandleEvent repeats the page's own override (Map.Sim.cs) — without it
    Blazor would put an automatic re-render back on every event in here, which the page turns off because
    the HUD's refresh belongs to OnTick's 200 ms throttle. The .razor.css beside this file carries no rules;
    it exists to give the component Map.razor.css's scope (#251 item 3 moves the rules).

    The `@{ }` around the fences is not decoration: the block used to sit inside `@if (…) { … }`,
    which is a Razor CODE block, and a code block is where its first statements are legal. The braces
    put the moved lines back in the context they were written in; they are outside the fences, so what
    the source guards read is still exactly the lines that were in the page.
*@
@{
@* ── MARKUP BEGINS · verbatim from Map.razor ─────────────────────────────────────────────────────────── *@
        <div class="convergence-backdrop" @onclick="() => Dismiss(CloseGroundLesson)">
            @* THE FIRST-GROUND FAMILY, and the one place in this wave where the button MOVES IN THE DOM.
               It was typed above `.ground-lesson-foot` and put back under it by `order: 1` (#735's block:
               "the footnote keeps its place in the prose, above the way on"). Bare draws the dismiss as the
               card's last child, which is where that rule already puts it on the screen — so the pixel is
               unchanged and the DOM finally reads the way the CSS comment says it does. *@
            <OverlayShell class="convergence-card ground-lesson-card" StopClicks="true"
                          Frame="OverlayFrame.Bare" Dismiss="OverlayDismiss.Close"
                          DismissClass="btn btn-light convergence-close" DismissFace="@GroundLesson.Dismiss"
                          OnClose="() => Dismiss(CloseGroundLesson)">
                <div class="convergence-stamp">@GroundLesson.Stamp</div>
                <div class="convergence-head">@GroundLesson.Head</div>
                <div class="ground-lesson-levers">
                    @foreach (GroundLesson.Lever lever in GroundLesson.Levers)
                    {
                        <div class="ground-lesson-lever">
                            <span class="ground-lesson-key">@lever.Key</span>
                            <span class="ground-lesson-lever-text">
                                <span class="ground-lesson-lever-title">@lever.Title</span>
                                <span class="ground-lesson-lever-what">@lever.What</span>
                            </span>
                        </div>
                    }
                </div>
                <ul class="ground-lesson-laws">
                    @foreach (string law in GroundLesson.Laws)
                    {
                        <li>@law</li>
                    }
                </ul>
                <div class="ground-lesson-foot">@GroundLesson.Foot</div>
            </OverlayShell>
        </div>
@* ── MARKUP ENDS ─────────────────────────────────────────────────────────────────────────────────────── *@
}

@code {
    /// <summary>the page's `void CloseGroundLesson()`</summary>
    [Parameter] public Action CloseGroundLesson { get; set; } = default!;
    /// <summary>the page's `Task Dismiss(Action close)`</summary>
    [Parameter] public Func<Action, Task> Dismiss { get; set; } = default!;

    // The page's own event dispatch, repeated: no automatic re-render per event.
    Task IHandleEvent.HandleEventAsync(EventCallbackWorkItem callback, object? arg) => callback.InvokeAsync(arg);
}
